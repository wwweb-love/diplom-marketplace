import styled from "styled-components"

const SectionBasketResultContainer = ({ className }) => {
    return (
        <div className={className}>
            SectionBasketResult
        </div>
    )
}

export const SectionBasketResult = styled(SectionBasketResultContainer)``